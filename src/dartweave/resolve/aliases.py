"""별칭 사전 — 표기 차이로만 안 붙는 법인을 손으로 이어준다.

왜 손으로 넣는가:
  자동 매칭은 오탐을 낸다. 실측에서 후보 5건 중 1건이 틀렸다 —
  `부산조선해양기자재공업협동조합`(선박기자재 조합)이 접두 일치로 `부산조선`
  (별개 회사)에 붙었다. 잘못된 별칭은 **서로 다른 두 회사를 한 노드로 합치고**,
  그 오염은 군집·중심성까지 조용히 흘러간다. 미해소로 남는 편이 낫다.

왜 사전이 작아도 되는가:
  미해소 법인의 대부분은 애초에 corpCode 에 없는 대상(해외 자회사·투자조합·특별계정)
  이라 별칭으로 풀 수 없고, 그건 `classify.py` 가 분모에서 뺀다. 사전은 **분류로도
  못 거르고 정규화로도 안 붙는 잔여**만 맡는다. 실측 표본에서 잔여 24종 중 4종.

키는 `normalize_name()` 을 통과한 형태로 넣는다 — `Resolver` 가 정규화 후 조회한다.
"""
from __future__ import annotations

# 전부 corpCode 정식명칭과 대조해 확인했다 (data/corpcode.json, 110,838건).
SEED_ALIASES: dict[str, str] = {
    # corpCode 는 약칭으로 등재돼 있는데 공시는 정식명을 쓴다.
    "삼성생명보험": "00126256",           # corpCode: 삼성생명
    # 표 각주 기호 '(*1)' 는 상호의 일부가 아니다.
    "삼성벤처투자(*1)": "00301574",       # corpCode: 삼성벤처투자
    # 옛 상호 병기 + 각주.
    "삼성글로벌리서치(구,삼성경제연구소)(*1)": "00217798",  # corpCode: 삼성글로벌리서치
    # 주식 종류 표기가 상호에 붙어 온 경우.
    "코람코가치투자제4의6호위탁관리자부동산투자회사(우선주)": "01885958",
}


def load_aliases() -> dict[str, str]:
    """Resolver 에 넣을 별칭 표. 사본을 준다 — 호출부가 덧붙여도 씨앗은 안 변한다."""
    return dict(SEED_ALIASES)
